apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: replicaset2
spec:
  members: 3
  version: 6.0.5-ent
  type: ReplicaSet

  opsManager:
    configMapRef:
      name: my-project
  credentials: organization-secret

  persistent: false
